# mini-app

Fixture project for the routing evals. Deliberately small, deliberately boring:
the point is that answering a "where is X" question about it requires reading a
few files, not that the code is interesting.
