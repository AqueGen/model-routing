A batch reaches applyTaxPolicy with its outcome field set to the string "warn", and context.lenient is false. What does applyTaxPolicy return?

Answer with the returned value alone on the first line, then say whether that is what the author of TAX_POLICY appears to have intended.
