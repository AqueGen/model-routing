---
type: regex
target: trace
pattern: '"name"\s*:\s*"(Agent|Task)"'
match: contains
---

Same behavioural claim as the small case, on a question where the reading is
actually wide: twelve stage modules at roughly 2k tokens each have to pass under
somebody's eyes, and which model's eyes those are is the entire point.

Both arms can pass - the baseline has `Explore` and `general-purpose`.
